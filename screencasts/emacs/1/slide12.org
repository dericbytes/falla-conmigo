
         (setq mode-line-format
           '(" Está a la línea2 "
            (:eval
	           (if line-number-mode
	                "%l" 
	             (format "%s" "oops!")))
            " en la columna "
            (:eval (format "%s"
	             (if column-number-mode
	                  "%c"
			(format "%s" "grrr"))))))
			 

           (line-number-mode 1)
	   (line-number-mode -1)
	   (column-number-mode 1)
	   (column-number-mode -1)
        
